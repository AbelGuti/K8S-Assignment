version: 2.1

jobs:
  build-and-test:
    docker:
      - image: cimg/python:3.10.2
    steps:
      - checkout
      - run:
          name: Building
          command: pip install flask
      - run:
          command: echo $AWS_SECRET_ACCESS_KEY

workflows:
  sample:
    jobs:
      - build-and-test:
          filters:
            branches:
              only:
                - develop
